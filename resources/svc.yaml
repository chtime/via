apiVersion: v1
kind: Service
metadata:
  labels:
    app: via
  name: via
spec:
  selector:
    app: via
  type: ClusterIP
  ports:
    - name: "8000"
      port: 8000
      protocol: TCP
      targetPort: 8000
